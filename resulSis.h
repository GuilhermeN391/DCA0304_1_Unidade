struct matrizes gauss(struct matrizes a, struct matrizes b, struct matrizes c);
/// Função que retorna o senh da variavel a inserida
float senh(float a);
/// Função para dar o valor de pi,uma vez que
/// no c não tem esse número na sua biblioteca
float pi();
float ana(int n, float coorX, float coorY);
struct matrizes multiana(int an);

